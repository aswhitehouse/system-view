export class EdgeDetail {
    constructor(id, source, target, animated) {
        this.id = id;
        this.source = source;
        this.target = target;
        this.animated = animated;
    }
}

export default EdgeDetail;